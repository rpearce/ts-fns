---
"@rpearce/ts-fns": minor
---

Add `ifElse` and `ifElseU`: a branching combinator where the predicate and both branches may each be a static value or a function of the data args — `ifElse(pred, onTrue, onFalse)(...data)`.
